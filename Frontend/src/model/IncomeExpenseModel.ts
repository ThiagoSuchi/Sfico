export interface Income {
    id: string
    valor: string,
    categoria: string,
    descricao: string,
    data: Date
}

export interface Expense {
    id: string
    valor: string,
    categoria: string,
    descricao: string,
    data: Date
}